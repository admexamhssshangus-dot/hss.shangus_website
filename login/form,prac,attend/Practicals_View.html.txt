<div id="practicalsView" class="hidden">

    <!-- [MODIFIED] Client-side permission handling instead of server-side tags -->
<div id="practicalsAdminGateway" class="gateway-sticky hidden">
    <div class="gateway-bar">
        <div class="gateway-container">
            <div class="gateway-label">School Services</div>
            <div class="gateway-items">
                <button onclick="switchPortal('attendance')" class="gateway-btn"
                    style="--accent: var(--secondary);">
                    <span class="material-icons">how_to_reg</span><span>Attendance</span>
                </button>
                <button onclick="switchPortal('automation')" class="gateway-btn"
                    style="--accent: var(--warning);">
                    <span class="material-icons">auto_fix_high</span><span>Automation</span>
                </button>
                <button onclick="switchPortal('advanced_reports')" class="gateway-btn"
                    style="--accent: #06b6d4;">
                    <span class="material-icons">analytics</span><span>Reports</span>
                </button>
                <button onclick="switchPortal('fund_distribution')" class="gateway-btn"
                    style="--accent: #ec4899;">
                    <span class="material-icons">payments</span><span>Distribution</span>
                </button>
            </div>
        </div>
    </div>
</div>

    <div class="app-container" id="appRoot">
        <div id="appContent"></div>
    </div>
    <!-- NEW: Dashboard update notification -->
    <div id="dashboardUpdateNotification" class="dashboard-update-notification">Dashboard updated</div>
    <!-- Main Modal -->
    <div id="prac_modal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3 id="prac_modalTitle" class="modal-title"></h3>
                <button id="prac_modalClose" class="modal-close" onclick="prac_hideModal()">&times;</button>
            </div>
            <div id="prac_modalBody" class="modal-body"></div>
            <div id="prac_modalFooter" class="modal-footer"></div>
        </div>
    </div>
    <!-- Container for the print-friendly certificate -->
    <div id="print-certificate-container" class="print-only"></div>
    
</div>