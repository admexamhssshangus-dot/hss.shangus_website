- [x] Implement `GET /api/messages` and `POST /api/messages` in `src/setupProxy.js`, persisting to `public/slides/messages.json`
- [x] Update `src/pages/AdminPortal.jsx` so “Apply & Save” always writes directory files (`settings.json`, `notices.txt`, `faculty.json`, `admins.json`) even if Firestore save fails
- [x] Run `npm run build` to ensure compilation success after edits
- [x] Re-scan for any remaining missing endpoints/refs and confirm behavior
