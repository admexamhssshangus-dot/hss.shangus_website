# Project Rules

## Git Workflow Rule
- **NEVER execute `git push` commands**.
- The user handles all `git push` operations manually.
- The assistant may edit files, test, and build locally, but MUST NEVER push changes to remote Git repositories.
